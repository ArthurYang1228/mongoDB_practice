from django.test import TestCase

# Create your tests here.
import pymongo


# if __name__ == "__main__":
#     client = pymongo.MongoClient(host='localhost', port=27017)
#     db = client['test']
#     grade = db['grade_stat']
#     results = grade.find({'category':'D', 'amount':1}).count()
#     num = 0
#     print(results)

    # for r in results:
    #     if num >10:
    #         break
    #     print(r)
    #     num +=1
    # print('' or 0)
